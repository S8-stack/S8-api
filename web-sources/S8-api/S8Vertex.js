

/**
 * 
 */
export class S8Vertex {

	
	/**
	 * 
	 * @param {string} methodName 
	 */
	runVoid(methodName) { 
		/* to be overridden */
		console.log(`runVoid method : ${methodName} with no args MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {boolean} argValue 
	 */
	runBool8(methodName, argValue) {
		/* to be overridden */
		console.log(`runBool8 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runBool8Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runBool8Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runUInt8(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt8 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runUInt8Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt8Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runUInt16(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt16 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runUInt16Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt16Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runUInt32(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt32 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runUInt32Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt32Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runUInt64(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt64 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runUInt64Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runUInt64Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt8(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt8 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runInt8Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt8Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt16(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt16 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runInt16Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt16Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt32(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt32 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt32Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt32Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt64(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt64 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runInt64Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runInt64Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runFloat32(methodName, argValue) {
		/* to be overridden */
		console.log(`runFloat32 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runFloat32Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runFloat32Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runFloat64(methodName, argValue) {
		/* to be overridden */
		console.log(`runFloat64 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}



	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runFloat64Array(methodName, argValue) {
		/* to be overridden */
		console.log(`runFloat64Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {number} argValue 
	 */
	runStringUTF8(methodName, argValue) {
		/* to be overridden */
		console.log(`runStringUTF8 method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}


	/**
	 * 
	 * @param {string} methodName 
	 * @param {Array} argValue 
	 */
	runStringUTF8Array(methodName, argValue) {
		/* to be overridden */
		console.error(`runStringUTF8Array method : ${methodName} with arg = ${argValue} MUST be overriden!`);
	}





	/**
	 * 
	 * @param {string} providerName 
	 * @param {string} filename 
	 */
	downloadRaw(providerName, filename) {
		/* to be overridden */
		console.log(`trigger downloadRaw : from ${providerName}, get = ${filename}`);
	}



}